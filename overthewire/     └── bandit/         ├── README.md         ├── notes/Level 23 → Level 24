# Level 23 → Level 24

## Objective
Retrieve the password for `bandit24` by inspecting a system `cron` job and creating a custom shell script.
## Concepts Learned
- Cron job scheduling (`/etc/cron.d/`)
- Writing bash payload scripts to extract restricted files
- File permission configuration (`chmod 777`) for execution by background daemons

## Commands Used
`cat`, `mkdir`, `chmod`, `nano`, `cp`, `ls`


